# Workstream: Slice 15: cross-correlation-on-envelope measurement upgrade (50-100x precision)

_Completion record recorded by Maverick on 2026-05-31T23:59:45.744Z._

- Workstream: f6a4f213-852b-494f-a96e-8d2353e6611f
- Completed by: http
- Epic: ultrasonic-runtime-sync
- Branch: maverick/syncsonic/ultrasonic/slice-15-cross-correlation-on-envelope-measurement-upgrade-50-100x-precision-f6a4f213
- Summary: Operator-confirmed verification (software-only) passed.
- Planned slice: Slice 15: Replace `_demodulated_envelope_scan`'s threshold-crossing+slope landmark finder with cross-correlation against a synthesized envelope template, add quadratic sub-sample interpolation on the correlation peak, emit `measurement_precision_us` in `burst_pattern_arrival`, and share the interpolation utility with `analyze_lag.py` via a new `correlation.py` module. This is a pure software change — no filter C code, no BLE changes, no new systemd units.
- Latest turn: I’ll treat this as a backend/hardware-affecting ultrasonic-runtime-sync slice, so I’m using the repo’s verification workflow and Pi validation skill guidance before claiming completion. I’ll first inspect the existing measurement code and workstream docs, then patch the smallest surface needed.

The sandbox wrapper is failing before commands run (`bwrap: loopback: Failed RTM_NEWADDR`), so I need to rerun essential repo reads outside the sandbox. I’ll keep those read-only and scoped to the exe...
- Verification: pass (ready-for-review)
- Last next action: Verify the changes before moving to review.
